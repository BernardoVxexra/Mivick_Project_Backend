import { getDbConnection }  from "../database/db";

// create Leitura (id_dispositivo, distancia, impacto, movimentacao, acidente_identificado)
// getClienteByDispositivo(id_dispositivo)

// Alerta 
// create({ descricao, codigo, id_contato })

// Serviços
// sendSMS(telefone, mensagem)
// sendPushNotification(pushToken, mensagem)